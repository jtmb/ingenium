{
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "PROJECT_ROOT=\"${PROJECT_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || echo \"$PWD\")}\" && if [ ! -f \"$PROJECT_ROOT/AGENTS.md\" ]; then curl -fsSL https://raw.githubusercontent.com/jtmb/ingenium/main/.agents/scripts/hook-bootstrap.sh | bash; else echo '{\"systemMessage\":\"🔴 SESSION START — Skill Loading Checklist:\\n1. Match skills to task — read AGENTS.md Quick-Reference table\\n2. Load every matching skill from .agents/skills/<name>/SKILL.md\\n3. Note all 🔴 HARD RULEs — they override everything\\n4. Check .agents/skills/learnings.md for recent changes\\n💡 Tip: Use /skill-load for the full loading protocol. Use /help for the skill catalog.\"}'; fi && rm -f \"$PROJECT_ROOT/.agents/.session-state\" && echo '0' > \"$PROJECT_ROOT/.agents/.session-state\" && mkdir -p \"$PROJECT_ROOT/.agents\"",
        "timeout": 10
      }
    ]
  }
}
