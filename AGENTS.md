# AGENTS.md — Skill System Index

This project uses a **skill-based AI conventions system**. All rules live in `.agents/skills/`, not in this file.

**Start here:** invoke the `always-read-agents` skill (or `/always-read-agents` in VS Code chat) to load all applicable conventions.

| What you need | Where to find it |
|---------------|-----------------|
| Core mandatory rules | `.agents/skills/generic-conventions/SKILL.md` |
| Framework-specific rules | `.agents/skills/{framework}-conventions/SKILL.md` |
| Project identity & context | `.agents/skills/repo-context/SKILL.md` |
| Full documentation | `docs/` and `USAGE.md` |

