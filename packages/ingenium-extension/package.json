{
  "name": "@ingenium/extension",
  "version": "1.0.0",
  "description": "OpenCode client-side package — MCP server, observer plugin, skill-sync plugin. Everything needed to connect OpenCode to an Ingenium API. Install with: npx -y @ingenium/extension",
  "type": "module",
  "main": "index.js",
  "bin": {
    "@ingenium/extension": "dist/scripts/mcp-server.js"
  },
  "files": ["index.js", "observer.js", "observer-core.js", "skill-sync.js", "auto-observer.js", "onboarding-sync.js", "resource-sync.js", "project-resolver.js", "dist/"],
  "scripts": {
    "build": "npm run build --workspace=ingenium-server && rm -rf dist && cp -r ../../services/ingenium-server/dist dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "@opencode-ai/plugin": "*"
  }
}
