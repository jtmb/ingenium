# ── Environment / Secrets ─────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
*.cred
*.credential

# ── AI-generated files ────────────────────────────────────
.agents/skills/*/SKILL.md.bak
.agents.old/
.ingenium/

# ── Dependencies ──────────────────────────────────────────
node_modules/
__pycache__/
*.pyc

# ── OS Files ──────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Kaban board (local state) ─────────────────────────────
.kaban/

# ── Playwright test artifacts ────────────────────────────
playwright-report/
test-results/
tests/test-results/
playwright/.cache/

# ── Build outputs ─────────────────────────────────────────
.next/
dist/
build/
*.tsbuildinfo

# ── Learnings — DB-managed only, not on disk ──────────────
.agents/skills/learnings.md
.opencode/skills/learnings.md

# ── Resource sync state — local manifest, not for sharing ─
.opencode/.ingenium-sync-state.json

# ── Existing entries ──────────────────────────────────────
.agents/.lm-studio-env
.vscode
temp

# ── SQLite runtime files ──────────────────────────────────
/data*
*.db
*.db-shm
*.db-wal

# ── Migration files are tracked — counteracts /data* ─────
!/packages/ingenium-core/data/migrations/

# ── Personal backup files ─────────────────────────────────
personal-space-backup.json

# ── OpenCode session logs ─────────────────────────────────
session-*.md
session_*.md

# ── Playwright MCP artifacts ──────────────────────────────
.playwright-mcp/

# YAML page snapshots (Playwright output)
*.snapshot.yml

# ── Plans and scratch notes ───────────────────────────────
next-steps-plan/
email-plan.md
*.plan.md
plan.md
servers-page.yml
mail-page-screenshot.png
mail-inbox-loaded.png
*.png
