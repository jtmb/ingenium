{
  "name": "ingenium-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": ["dist/", "README.md"],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "prepack": "npm run build",
    "start": "node dist/scripts/mcp-server.js",
    "dev": "tsx watch scripts/mcp-server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0",
    "pino": "^9.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "vitest": "^2.0.0"
  }
}
